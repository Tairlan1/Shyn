scikit-learn>=1.3
numpy>=1.24
scipy>=1.10
joblib>=1.3
chardet>=5.0

# Веб-приложение (app.py) и REST API для внешних фронтендов (api_analyze.py,
# используется UniPlatform):
Flask>=3.0
flask-cors>=4.0   # необязательно - при отсутствии api_analyze.py сам
                   # включает CORS-заголовки вручную (см. try/except там)

# Извлечение текста из загружаемых студентами файлов (doc_extract.py):
python-docx>=1.1
pdfplumber>=0.11

# Необязательно, но желательно - улучшает разбиение на предложения:
nltk>=3.8
